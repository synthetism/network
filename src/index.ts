export { Network, type NetworkConfig, type NetworkProps, type RequestOptions } from './network.unit.js';

// Re-export HTTP types for convenience
export type { RequestResult, HttpResponse } from '@synet/http';
